#include "main.h"
#include <stdio.h>
/**
 * myname - Program that prints its name
 * @argc: argument count
 * @argv: argument array
 * Return: Integer
 */
int myname(int argc, char *argv[])
{
	printf("%s\n", *arg[0]);

	if (argc == 1)
	{
		return (0);
	}
	else
		return (0);
}
