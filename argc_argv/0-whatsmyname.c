#include <stdio.h>
/**
 * myname - Program that prints its name
 * @argc: argument count
 * @argv: argument array
 * Return: Integer
 */
int myname(int argc, char *argv[])
{
	if (argc == 0)
	{
		printf("%s\n", argv[0]);
	}
	else
	{
		printf("%s\n", argv[0]);
	}
	
	return (0);
}
